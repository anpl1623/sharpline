// Package gen provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.8.0 DO NOT EDIT.
package gen

import (
	"time"
)

// Defines values for AccountBalanceAccountKind.
const (
	UserCash   AccountBalanceAccountKind = "user_cash"
	UserEscrow AccountBalanceAccountKind = "user_escrow"
)

// Valid indicates whether the value is a known member of the AccountBalanceAccountKind enum.
func (e AccountBalanceAccountKind) Valid() bool {
	switch e {
	case UserCash:
		return true
	case UserEscrow:
		return true
	default:
		return false
	}
}

// Defines values for AccountStatus.
const (
	AccountStatusActive       AccountStatus = "active"
	AccountStatusClosed       AccountStatus = "closed"
	AccountStatusSelfExcluded AccountStatus = "self_excluded"
	AccountStatusSuspended    AccountStatus = "suspended"
)

// Valid indicates whether the value is a known member of the AccountStatus enum.
func (e AccountStatus) Valid() bool {
	switch e {
	case AccountStatusActive:
		return true
	case AccountStatusClosed:
		return true
	case AccountStatusSelfExcluded:
		return true
	case AccountStatusSuspended:
		return true
	default:
		return false
	}
}

// Defines values for BalanceResponseCurrency.
const (
	PLAY BalanceResponseCurrency = "PLAY"
)

// Valid indicates whether the value is a known member of the BalanceResponseCurrency enum.
func (e BalanceResponseCurrency) Valid() bool {
	switch e {
	case PLAY:
		return true
	default:
		return false
	}
}

// Defines values for BalanceResponseMinorUnitsPerMajor.
const (
	N100 BalanceResponseMinorUnitsPerMajor = 100
)

// Valid indicates whether the value is a known member of the BalanceResponseMinorUnitsPerMajor enum.
func (e BalanceResponseMinorUnitsPerMajor) Valid() bool {
	switch e {
	case N100:
		return true
	default:
		return false
	}
}

// Defines values for BookKind.
const (
	External  BookKind = "external"
	Synthetic BookKind = "synthetic"
)

// Valid indicates whether the value is a known member of the BookKind enum.
func (e BookKind) Valid() bool {
	switch e {
	case External:
		return true
	case Synthetic:
		return true
	default:
		return false
	}
}

// Defines values for ErrorCode.
const (
	ErrorCodeAccountNotActive   ErrorCode = "account_not_active"
	ErrorCodeAlreadyExists      ErrorCode = "already_exists"
	ErrorCodeBadRequest         ErrorCode = "bad_request"
	ErrorCodeConflict           ErrorCode = "conflict"
	ErrorCodeForbidden          ErrorCode = "forbidden"
	ErrorCodeInternal           ErrorCode = "internal"
	ErrorCodeInvalidCredentials ErrorCode = "invalid_credentials"
	ErrorCodeInvalidCursor      ErrorCode = "invalid_cursor"
	ErrorCodeInvalidParameter   ErrorCode = "invalid_parameter"
	ErrorCodeInvalidTotpCode    ErrorCode = "invalid_totp_code"
	ErrorCodeNotFound           ErrorCode = "not_found"
	ErrorCodeRateLimited        ErrorCode = "rate_limited"
	ErrorCodeTotpRequired       ErrorCode = "totp_required"
	ErrorCodeUnauthenticated    ErrorCode = "unauthenticated"
	ErrorCodeUnprocessable      ErrorCode = "unprocessable"
)

// Valid indicates whether the value is a known member of the ErrorCode enum.
func (e ErrorCode) Valid() bool {
	switch e {
	case ErrorCodeAccountNotActive:
		return true
	case ErrorCodeAlreadyExists:
		return true
	case ErrorCodeBadRequest:
		return true
	case ErrorCodeConflict:
		return true
	case ErrorCodeForbidden:
		return true
	case ErrorCodeInternal:
		return true
	case ErrorCodeInvalidCredentials:
		return true
	case ErrorCodeInvalidCursor:
		return true
	case ErrorCodeInvalidParameter:
		return true
	case ErrorCodeInvalidTotpCode:
		return true
	case ErrorCodeNotFound:
		return true
	case ErrorCodeRateLimited:
		return true
	case ErrorCodeTotpRequired:
		return true
	case ErrorCodeUnauthenticated:
		return true
	case ErrorCodeUnprocessable:
		return true
	default:
		return false
	}
}

// Defines values for EventKind.
const (
	EventKindMatch    EventKind = "match"
	EventKindOutright EventKind = "outright"
)

// Valid indicates whether the value is a known member of the EventKind enum.
func (e EventKind) Valid() bool {
	switch e {
	case EventKindMatch:
		return true
	case EventKindOutright:
		return true
	default:
		return false
	}
}

// Defines values for EventStatus.
const (
	EventStatusCancelled EventStatus = "cancelled"
	EventStatusEnded     EventStatus = "ended"
	EventStatusLive      EventStatus = "live"
	EventStatusPostponed EventStatus = "postponed"
	EventStatusScheduled EventStatus = "scheduled"
	EventStatusSettled   EventStatus = "settled"
	EventStatusSuspended EventStatus = "suspended"
)

// Valid indicates whether the value is a known member of the EventStatus enum.
func (e EventStatus) Valid() bool {
	switch e {
	case EventStatusCancelled:
		return true
	case EventStatusEnded:
		return true
	case EventStatusLive:
		return true
	case EventStatusPostponed:
		return true
	case EventStatusScheduled:
		return true
	case EventStatusSettled:
		return true
	case EventStatusSuspended:
		return true
	default:
		return false
	}
}

// Defines values for HistoryResolution.
const (
	N10s HistoryResolution = "10s"
	N15m HistoryResolution = "15m"
	N1d  HistoryResolution = "1d"
	N1h  HistoryResolution = "1h"
	N1m  HistoryResolution = "1m"
	N5m  HistoryResolution = "5m"
	N6h  HistoryResolution = "6h"
	Raw  HistoryResolution = "raw"
)

// Valid indicates whether the value is a known member of the HistoryResolution enum.
func (e HistoryResolution) Valid() bool {
	switch e {
	case N10s:
		return true
	case N15m:
		return true
	case N1d:
		return true
	case N1h:
		return true
	case N1m:
		return true
	case N5m:
		return true
	case N6h:
		return true
	case Raw:
		return true
	default:
		return false
	}
}

// Defines values for LimitKind.
const (
	LimitKindGrant   LimitKind = "grant"
	LimitKindLoss    LimitKind = "loss"
	LimitKindSession LimitKind = "session"
	LimitKindStake   LimitKind = "stake"
)

// Valid indicates whether the value is a known member of the LimitKind enum.
func (e LimitKind) Valid() bool {
	switch e {
	case LimitKindGrant:
		return true
	case LimitKindLoss:
		return true
	case LimitKindSession:
		return true
	case LimitKindStake:
		return true
	default:
		return false
	}
}

// Defines values for LimitPeriod.
const (
	LimitPeriodDay     LimitPeriod = "day"
	LimitPeriodMonth   LimitPeriod = "month"
	LimitPeriodSession LimitPeriod = "session"
	LimitPeriodWeek    LimitPeriod = "week"
)

// Valid indicates whether the value is a known member of the LimitPeriod enum.
func (e LimitPeriod) Valid() bool {
	switch e {
	case LimitPeriodDay:
		return true
	case LimitPeriodMonth:
		return true
	case LimitPeriodSession:
		return true
	case LimitPeriodWeek:
		return true
	default:
		return false
	}
}

// Defines values for MarketStatus.
const (
	MarketStatusClosed    MarketStatus = "closed"
	MarketStatusOpen      MarketStatus = "open"
	MarketStatusSettled   MarketStatus = "settled"
	MarketStatusSuspended MarketStatus = "suspended"
	MarketStatusVoided    MarketStatus = "voided"
)

// Valid indicates whether the value is a known member of the MarketStatus enum.
func (e MarketStatus) Valid() bool {
	switch e {
	case MarketStatusClosed:
		return true
	case MarketStatusOpen:
		return true
	case MarketStatusSettled:
		return true
	case MarketStatusSuspended:
		return true
	case MarketStatusVoided:
		return true
	default:
		return false
	}
}

// Defines values for MarketType.
const (
	Futures    MarketType = "futures"
	Moneyline  MarketType = "moneyline"
	PlayerProp MarketType = "player_prop"
	Spread     MarketType = "spread"
	Total      MarketType = "total"
)

// Valid indicates whether the value is a known member of the MarketType enum.
func (e MarketType) Valid() bool {
	switch e {
	case Futures:
		return true
	case Moneyline:
		return true
	case PlayerProp:
		return true
	case Spread:
		return true
	case Total:
		return true
	default:
		return false
	}
}

// Defines values for OddsFormat.
const (
	American   OddsFormat = "american"
	Decimal    OddsFormat = "decimal"
	Fractional OddsFormat = "fractional"
)

// Valid indicates whether the value is a known member of the OddsFormat enum.
func (e OddsFormat) Valid() bool {
	switch e {
	case American:
		return true
	case Decimal:
		return true
	case Fractional:
		return true
	default:
		return false
	}
}

// Defines values for SelectionRole.
const (
	SelectionRoleAway     SelectionRole = "away"
	SelectionRoleDraw     SelectionRole = "draw"
	SelectionRoleHome     SelectionRole = "home"
	SelectionRoleOutright SelectionRole = "outright"
	SelectionRoleOver     SelectionRole = "over"
	SelectionRoleUnder    SelectionRole = "under"
)

// Valid indicates whether the value is a known member of the SelectionRole enum.
func (e SelectionRole) Valid() bool {
	switch e {
	case SelectionRoleAway:
		return true
	case SelectionRoleDraw:
		return true
	case SelectionRoleHome:
		return true
	case SelectionRoleOutright:
		return true
	case SelectionRoleOver:
		return true
	case SelectionRoleUnder:
		return true
	default:
		return false
	}
}

// Defines values for SessionResponseTokenType.
const (
	Bearer SessionResponseTokenType = "Bearer"
)

// Valid indicates whether the value is a known member of the SessionResponseTokenType enum.
func (e SessionResponseTokenType) Valid() bool {
	switch e {
	case Bearer:
		return true
	default:
		return false
	}
}

// Account The whole profile. There is no name, address, date of birth, document,
// country or jurisdiction field, because this is not a licensed sportsbook
// and no such column exists (migration 00005). Their absence is deliberate.
type Account struct {
	CreatedAt time.Time `json:"created_at"`

	// Email An email address.
	//
	// The pattern is EXACTLY the database's `users_email_shape` CHECK -- one
	// `@` with non-empty, whitespace-free sides -- and no tighter. Stricter
	// regexes reject deliverable addresses (quoted local parts, single-label
	// domains) and the only real proof an address exists is sending mail to it.
	//
	// Deliberately NOT `format: email`. That format makes oapi-codegen emit
	// `openapi_types.Email`, which drags github.com/oapi-codegen/runtime into a
	// module whose serving path otherwise has no third-party dependency at all,
	// in exchange for a type alias over `string` that validates nothing at
	// runtime. The pattern above is the validation, it is the same one the
	// database enforces, and it costs nothing.
	//
	// Stored lowercase-normalised; the database CHECK makes that an invariant
	// rather than a convention, so a plain unique index is a correct
	// case-insensitive uniqueness constraint.
	//
	//
	// Example: sharp@example.test
	Email EmailAddress `json:"email"`

	// Id A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	Id     EntityID      `json:"id"`
	Status AccountStatus `json:"status"`

	// TotpEnrolled True only for a CONFIRMED enrolment. An unconfirmed one is not a second factor.
	TotpEnrolled bool `json:"totp_enrolled"`
}

// AccountBalance defines model for AccountBalance.
type AccountBalance struct {
	AccountKind AccountBalanceAccountKind `json:"account_kind"`

	// BalanceMinor An amount in MINOR UNITS (cents), as an integer. Never a float, never a
	// string. The bound is 2^53-1, which is both the largest integer a float64
	// holds exactly and JavaScript's `Number.MAX_SAFE_INTEGER`, and is the same
	// bound the database CHECK constraints enforce -- so a JSON number is
	// lossless for every value that can exist.
	//
	//
	// Example: 250000
	BalanceMinor MoneyMinor `json:"balance_minor"`

	// EntryCount Ledger entries folded to produce this balance. Zero means the account
	// has never moved -- a different fact from "moved and nets to zero",
	// and the only thing that distinguishes them.
	EntryCount int64 `json:"entry_count"`
}

// AccountBalanceAccountKind defines model for AccountBalance.AccountKind.
type AccountBalanceAccountKind string

// AccountStatus defines model for AccountStatus.
type AccountStatus string

// BalanceResponse defines model for BalanceResponse.
type BalanceResponse struct {
	AsOf time.Time      `json:"as_of"`
	Cash AccountBalance `json:"cash"`

	// Currency Not a real currency and deliberately not an ISO 4217 code. Labelling
	// play money `USD` is the first step toward a client treating it as
	// money, which CLAUDE.md section 0 forbids.
	Currency BalanceResponseCurrency `json:"currency"`
	Escrow   AccountBalance          `json:"escrow"`

	// MinorUnitsPerMajor Divide by this only for DISPLAY. Never do arithmetic in major units.
	MinorUnitsPerMajor BalanceResponseMinorUnitsPerMajor `json:"minor_units_per_major"`

	// TotalMinor cash + escrow. The user's total position.
	TotalMinor MoneyMinor `json:"total_minor"`
}

// BalanceResponseCurrency Not a real currency and deliberately not an ISO 4217 code. Labelling
// play money `USD` is the first step toward a client treating it as
// money, which CLAUDE.md section 0 forbids.
type BalanceResponseCurrency string

// BalanceResponseMinorUnitsPerMajor Divide by this only for DISPLAY. Never do arithmetic in major units.
type BalanceResponseMinorUnitsPerMajor int32

// BestPrice defines model for BestPrice.
type BestPrice struct {
	// BookId A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	BookId EntityID `json:"book_id"`

	// BookSlug Example: nfl
	BookSlug Slug `json:"book_slug"`

	// DecimalOdds Decimal odds -- the total return per unit staked, stake included. THE
	// CANONICAL representation. American and fractional are derived and lossy.
	//
	//
	// Example: 1.909
	DecimalOdds DecimalOdds `json:"decimal_odds"`
	Display     *string     `json:"display,omitempty"`
	ObservedAt  time.Time   `json:"observed_at"`

	// Role Exactly `domain.SelectionRole`.
	Role SelectionRole `json:"role"`

	// SelectionId A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	SelectionId EntityID `json:"selection_id"`
}

// BoardEntry defines model for BoardEntry.
type BoardEntry struct {
	Event EventSummary `json:"event"`

	// Markets The event's markets with their selections and current prices.
	Markets []Market `json:"markets"`
}

// BoardPage defines model for BoardPage.
type BoardPage struct {
	// AsOf When this page was assembled. A client computes staleness against
	// this rather than against its own clock, so a skewed browser clock
	// cannot make a fresh board look stale.
	AsOf time.Time    `json:"as_of"`
	Data []BoardEntry `json:"data"`

	// Page Keyset pagination. There is no total count and there will not be one:
	// counting an unbounded, continuously-written set costs a full scan on
	// every page and the number is stale before it is serialised.
	Page PageInfo `json:"page"`
}

// Book defines model for Book.
type Book struct {
	// Id A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	Id EntityID `json:"id"`

	// IsReference The sharp book the pricer devigs against to produce fair value. Every
	// EV number in this system is relative to this book, so a client that
	// renders EV must be able to name it.
	IsReference bool `json:"is_reference"`

	// Kind Exactly `domain.BookKind`. `synthetic` is the in-house stochastic market
	// maker: a quote from it is a statement about a random number generator and
	// every surface that renders one must be able to label it as such.
	Kind BookKind `json:"kind"`
	Name string   `json:"name"`

	// Slug Example: nfl
	Slug Slug `json:"slug"`
}

// BookKind Exactly `domain.BookKind`. `synthetic` is the in-house stochastic market
// maker: a quote from it is a statement about a random number generator and
// every surface that renders one must be able to label it as such.
type BookKind string

// BookPage defines model for BookPage.
type BookPage struct {
	Data []Book `json:"data"`

	// Page Keyset pagination. There is no total count and there will not be one:
	// counting an unbounded, continuously-written set costs a full scan on
	// every page and the number is stale before it is serialised.
	Page PageInfo `json:"page"`
}

// BookQuote defines model for BookQuote.
type BookQuote struct {
	// BookId A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	BookId EntityID `json:"book_id"`

	// BookKind Exactly `domain.BookKind`. `synthetic` is the in-house stochastic market
	// maker: a quote from it is a statement about a random number generator and
	// every surface that renders one must be able to label it as such.
	BookKind BookKind `json:"book_kind"`
	BookName string   `json:"book_name"`

	// BookSlug Example: nfl
	BookSlug    Slug `json:"book_slug"`
	IsReference bool `json:"is_reference"`

	// Overround `sum(1/decimal_odds) - 1` over this book's quotes on the market --
	// the book's margin. Null when the book has not quoted every selection,
	// because an overround computed over a partial market is not a margin,
	// it is a smaller number that looks like a better price.
	Overround *float64         `json:"overround,omitempty"`
	Quotes    []SelectionQuote `json:"quotes"`
}

// Competitor defines model for Competitor.
type Competitor struct {
	// Id A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	Id   *EntityID `json:"id,omitempty"`
	Name string    `json:"name"`
}

// DecimalOdds Decimal odds -- the total return per unit staked, stake included. THE
// CANONICAL representation. American and fractional are derived and lossy.
//
// Example: 1.909
type DecimalOdds = float64

// EmailAddress An email address.
//
// The pattern is EXACTLY the database's `users_email_shape` CHECK -- one
// `@` with non-empty, whitespace-free sides -- and no tighter. Stricter
// regexes reject deliverable addresses (quoted local parts, single-label
// domains) and the only real proof an address exists is sending mail to it.
//
// Deliberately NOT `format: email`. That format makes oapi-codegen emit
// `openapi_types.Email`, which drags github.com/oapi-codegen/runtime into a
// module whose serving path otherwise has no third-party dependency at all,
// in exchange for a type alias over `string` that validates nothing at
// runtime. The pattern above is the validation, it is the same one the
// database enforces, and it costs nothing.
//
// Stored lowercase-normalised; the database CHECK makes that an invariant
// rather than a convention, so a plain unique index is a correct
// case-insensitive uniqueness constraint.
//
// Example: sharp@example.test
type EmailAddress = string

// EntityID A domain identifier. The charset is exactly `domain.validID`: `:` is
// excluded because WebSocket channels are `event:{id}` and a colon inside
// an identifier would make splitting a channel name ambiguous.
//
// Example: evt_nfl_2026_w1_kc_buf
type EntityID = string

// Error THE ONE ERROR SHAPE. Every non-2xx response in this API has exactly this
// body, so a client writes one decoder and one branch.
//
// `code` is a stable machine-readable token and is what a client should
// switch on; the HTTP status is a coarser classification of the same fact.
// `message` is a fixed human-readable string chosen from a closed set in
// Go. It is NEVER an error value, a driver message, a SQLSTATE, a stack
// frame, a file path or a query -- an error body is an untrusted output
// surface and everything a caller must not learn is logged under
// `request_id` and dropped here.
//
// `invalid_params` is present only for `400` and `422` and names the
// offending parameters so a form can highlight them. Its `reason` strings
// are equally fixed.
type Error struct {
	Code          ErrorCode       `json:"code"`
	InvalidParams *[]InvalidParam `json:"invalid_params,omitempty"`
	Message       string          `json:"message"`

	// RequestId Correlates this response with the server log line and the trace span
	// that produced it. Quote it in a bug report; it is the only handle on
	// what actually happened.
	RequestId string `json:"request_id"`
}

// ErrorCode defines model for Error.Code.
type ErrorCode string

// EventDetail defines model for EventDetail.
type EventDetail struct {
	AsOf    time.Time    `json:"as_of"`
	Event   EventSummary `json:"event"`
	League  League       `json:"league"`
	Markets []Market     `json:"markets"`
	Sport   Sport        `json:"sport"`
}

// EventKind Exactly `domain.EventKind`. `match` is a contest between two competitors;
// `outright` is a competition resolved among many runners and has no home
// or away side at all -- not empty ones.
type EventKind string

// EventStatus Exactly `domain.EventStatus`, same spelling, same members. The API does
// not get to invent a status the domain cannot represent, and the domain
// does not get to add one without changing this list.
type EventStatus string

// EventSummary defines model for EventSummary.
type EventSummary struct {
	AwayCompetitor *Competitor `json:"away_competitor,omitempty"`

	// Clock Live clock state. Absent entirely on an event that has not started.
	Clock          *GameClock  `json:"clock,omitempty"`
	HomeCompetitor *Competitor `json:"home_competitor,omitempty"`

	// Id A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	Id EntityID `json:"id"`

	// Kind Exactly `domain.EventKind`. `match` is a contest between two competitors;
	// `outright` is a competition resolved among many runners and has no home
	// or away side at all -- not empty ones.
	Kind EventKind `json:"kind"`

	// LeagueId A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	LeagueId EntityID `json:"league_id"`
	Name     string   `json:"name"`

	// ObservedAt The provider instant this event's state was observed at.
	ObservedAt     time.Time `json:"observed_at"`
	ScheduledStart time.Time `json:"scheduled_start"`
	Score          *Score    `json:"score,omitempty"`

	// Status Exactly `domain.EventStatus`, same spelling, same members. The API does
	// not get to invent a status the domain cannot represent, and the domain
	// does not get to add one without changing this list.
	Status EventStatus `json:"status"`
}

// GameClock Live clock state. Absent entirely on an event that has not started.
type GameClock struct {
	ElapsedSeconds *int64 `json:"elapsed_seconds,omitempty"`
	Period         *int32 `json:"period,omitempty"`
	Running        bool   `json:"running"`
}

// HistoryPoint One point of a line-movement series. On `resolution: raw` every field but
// `samples` holds the same stored quote and `samples` is 1, so a client
// renders one shape whether or not it asked for downsampling.
//
// Open/high/low/close rather than a mean: the mean of a line is a price
// nobody traded at, and CLV and steam detection are both computed from the
// endpoints of a window, not from its average.
type HistoryPoint struct {
	// At The bucket's start instant (`raw`: the quote's `observed_at`).
	At time.Time `json:"at"`

	// Close Decimal odds -- the total return per unit staked, stake included. THE
	// CANONICAL representation. American and fractional are derived and lossy.
	//
	//
	// Example: 1.909
	Close        DecimalOdds `json:"close"`
	CloseDisplay *string     `json:"close_display,omitempty"`

	// High Decimal odds -- the total return per unit staked, stake included. THE
	// CANONICAL representation. American and fractional are derived and lossy.
	//
	//
	// Example: 1.909
	High DecimalOdds `json:"high"`

	// Line The line at the bucket's close.
	Line *float64 `json:"line,omitempty"`

	// Low Decimal odds -- the total return per unit staked, stake included. THE
	// CANONICAL representation. American and fractional are derived and lossy.
	//
	//
	// Example: 1.909
	Low DecimalOdds `json:"low"`

	// Open Decimal odds -- the total return per unit staked, stake included. THE
	// CANONICAL representation. American and fractional are derived and lossy.
	//
	//
	// Example: 1.909
	Open    DecimalOdds `json:"open"`
	Samples int32       `json:"samples"`
}

// HistoryResolution defines model for HistoryResolution.
type HistoryResolution string

// HistorySeries defines model for HistorySeries.
type HistorySeries struct {
	// BookId A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	BookId EntityID `json:"book_id"`

	// BookSlug Example: nfl
	BookSlug Slug `json:"book_slug"`

	// From Inclusive.
	From   time.Time      `json:"from"`
	Points []HistoryPoint `json:"points"`

	// Resolution The resolution actually applied. Equal to the one requested, or `raw` if none was.
	Resolution HistoryResolution `json:"resolution"`

	// SelectionId A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	SelectionId EntityID `json:"selection_id"`

	// To Exclusive.
	To time.Time `json:"to"`
}

// InvalidParam defines model for InvalidParam.
type InvalidParam struct {
	// Name The query parameter, path parameter or JSON pointer at fault.
	Name   string `json:"name"`
	Reason string `json:"reason"`
}

// League defines model for League.
type League struct {
	// Id A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	Id   EntityID `json:"id"`
	Name string   `json:"name"`

	// Slug Example: nfl
	Slug Slug `json:"slug"`

	// SportId A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	SportId EntityID `json:"sport_id"`
}

// LeaguePage defines model for LeaguePage.
type LeaguePage struct {
	Data []League `json:"data"`

	// Page Keyset pagination. There is no total count and there will not be one:
	// counting an unbounded, continuously-written set costs a full scan on
	// every page and the number is stale before it is serialised.
	Page PageInfo `json:"page"`
}

// Limit defines model for Limit.
type Limit struct {
	// AmountMinor Set on money kinds (`grant`, `stake`, `loss`); null on `session`.
	AmountMinor *MoneyMinor `json:"amount_minor,omitempty"`

	// DurationSeconds Set on `session` only; null on money kinds.
	DurationSeconds *int32 `json:"duration_seconds,omitempty"`

	// EffectiveFrom Equal to `requested_at` for a tightening. Later for a loosening
	// serving its cooling-off period.
	EffectiveFrom time.Time `json:"effective_from"`

	// Id A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	Id EntityID `json:"id"`

	// InForce Whether `effective_from` has passed. A pending loosening is returned with `false`.
	InForce bool `json:"in_force"`

	// Kind Deliberately NOT `deposit`: this system has no deposits (CLAUDE.md
	// section 0). `grant`, `stake` and `loss` are spelled identically to the
	// ledger entry kinds they are evaluated against, so enforcing a limit is a
	// sum filtered by the same string with no translation table in between.
	Kind        LimitKind   `json:"kind"`
	Period      LimitPeriod `json:"period"`
	RequestedAt time.Time   `json:"requested_at"`
}

// LimitKind Deliberately NOT `deposit`: this system has no deposits (CLAUDE.md
// section 0). `grant`, `stake` and `loss` are spelled identically to the
// ledger entry kinds they are evaluated against, so enforcing a limit is a
// sum filtered by the same string with no translation table in between.
type LimitKind string

// LimitPage defines model for LimitPage.
type LimitPage struct {
	Data []Limit `json:"data"`

	// Page Keyset pagination. There is no total count and there will not be one:
	// counting an unbounded, continuously-written set costs a full scan on
	// every page and the number is stale before it is serialised.
	Page PageInfo `json:"page"`
}

// LimitPeriod defines model for LimitPeriod.
type LimitPeriod string

// LoginRequest defines model for LoginRequest.
type LoginRequest struct {
	// Email An email address.
	//
	// The pattern is EXACTLY the database's `users_email_shape` CHECK -- one
	// `@` with non-empty, whitespace-free sides -- and no tighter. Stricter
	// regexes reject deliverable addresses (quoted local parts, single-label
	// domains) and the only real proof an address exists is sending mail to it.
	//
	// Deliberately NOT `format: email`. That format makes oapi-codegen emit
	// `openapi_types.Email`, which drags github.com/oapi-codegen/runtime into a
	// module whose serving path otherwise has no third-party dependency at all,
	// in exchange for a type alias over `string` that validates nothing at
	// runtime. The pattern above is the validation, it is the same one the
	// database enforces, and it costs nothing.
	//
	// Stored lowercase-normalised; the database CHECK makes that an invariant
	// rather than a convention, so a plain unique index is a correct
	// case-insensitive uniqueness constraint.
	//
	//
	// Example: sharp@example.test
	Email    EmailAddress `json:"email"`
	Password string       `json:"password"`

	// TotpCode Required only if the account has a confirmed TOTP enrolment.
	TotpCode *string `json:"totp_code,omitempty"`
}

// Market defines model for Market.
type Market struct {
	// EventId A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	EventId EntityID `json:"event_id"`

	// Id A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	Id         EntityID    `json:"id"`
	Line       *float64    `json:"line,omitempty"`
	ObservedAt time.Time   `json:"observed_at"`
	Selections []Selection `json:"selections"`

	// Status Exactly `domain.MarketStatus`.
	Status MarketStatus `json:"status"`

	// Subject The player a prop market is about. Null on every other market type.
	Subject *string `json:"subject,omitempty"`

	// Type Exactly `domain.MarketType`.
	Type MarketType `json:"type"`
}

// MarketComparison defines model for MarketComparison.
type MarketComparison struct {
	AsOf time.Time `json:"as_of"`

	// Best The best available price on each selection, and where it is. This is
	// the input to arbitrage detection and it is computed here rather than
	// left to the client so that "best" means one thing across every
	// surface.
	Best []BestPrice `json:"best"`

	// Books Every book quoting this market, ordered by slug.
	Books  []BookQuote  `json:"books"`
	Market MarketHeader `json:"market"`
}

// MarketHeader defines model for MarketHeader.
type MarketHeader struct {
	// EventId A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	EventId EntityID `json:"event_id"`

	// Id A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	Id         EntityID  `json:"id"`
	Line       *float64  `json:"line,omitempty"`
	ObservedAt time.Time `json:"observed_at"`

	// Status Exactly `domain.MarketStatus`.
	Status  MarketStatus `json:"status"`
	Subject *string      `json:"subject,omitempty"`

	// Type Exactly `domain.MarketType`.
	Type MarketType `json:"type"`
}

// MarketStatus Exactly `domain.MarketStatus`.
type MarketStatus string

// MarketType Exactly `domain.MarketType`.
type MarketType string

// MoneyMinor An amount in MINOR UNITS (cents), as an integer. Never a float, never a
// string. The bound is 2^53-1, which is both the largest integer a float64
// holds exactly and JavaScript's `Number.MAX_SAFE_INTEGER`, and is the same
// bound the database CHECK constraints enforce -- so a JSON number is
// lossless for every value that can exist.
//
// Example: 250000
type MoneyMinor = int64

// OddsFormat defines model for OddsFormat.
type OddsFormat string

// PageInfo Keyset pagination. There is no total count and there will not be one:
// counting an unbounded, continuously-written set costs a full scan on
// every page and the number is stale before it is serialised.
type PageInfo struct {
	HasMore bool `json:"has_more"`

	// Limit The page size actually applied, which may be smaller than the one requested.
	Limit int32 `json:"limit"`

	// NextCursor Pass back as `cursor` for the next page. `null` when `has_more` is
	// false. Opaque -- do not parse it.
	NextCursor *string `json:"next_cursor,omitempty"`
}

// Price defines model for Price.
type Price struct {
	// BookId A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	BookId EntityID `json:"book_id"`

	// BookSlug Example: nfl
	BookSlug Slug `json:"book_slug"`

	// DecimalOdds Decimal odds -- the total return per unit staked, stake included. THE
	// CANONICAL representation. American and fractional are derived and lossy.
	//
	//
	// Example: 1.909
	DecimalOdds DecimalOdds `json:"decimal_odds"`

	// Display The price rendered in the requested `odds_format`. Present only when
	// `odds_format` is `american` or `fractional`; `decimal_odds` above is
	// always authoritative.
	Display *string `json:"display,omitempty"`

	// ImpliedProbability `1/decimal_odds`. WITH the book's vig still in it -- this is not a
	// fair probability and must not be labelled as one in a UI. Returned
	// because it is what a price cell's width or a bar chart is drawn from,
	// and computing it client-side is one more place to get the reciprocal
	// wrong.
	ImpliedProbability Probability `json:"implied_probability"`

	// IngestedAt When this system received it. `ingested_at - observed_at` is the provider-attributable share of staleness.
	IngestedAt time.Time `json:"ingested_at"`

	// Line The handicap or total this price was quoted at, when the market carries one.
	Line *float64 `json:"line,omitempty"`

	// ObservedAt The provider's own instant. The subtrahend in the staleness SLO.
	ObservedAt time.Time `json:"observed_at"`
}

// Probability defines model for Probability.
type Probability = float64

// RefreshRequest defines model for RefreshRequest.
type RefreshRequest struct {
	// RefreshToken The opaque refresh token from a previous session response. At least
	// 256 bits of `crypto/rand`; only its SHA-256 is stored, so a database
	// leak yields no working tokens.
	RefreshToken string `json:"refresh_token"`
}

// RegisterRequest defines model for RegisterRequest.
type RegisterRequest struct {
	// Email Normalised to lowercase before storage; the database CHECK makes that an invariant, not a convention.
	Email EmailAddress `json:"email"`

	// Password Minimum 12 characters and no composition rules. Length beats
	// character-class requirements, which push users toward `Password1!`
	// and toward reuse. Hashed with argon2id; never logged, never traced,
	// never echoed.
	Password string `json:"password"`
}

// Score defines model for Score.
type Score struct {
	Away int32 `json:"away"`
	Home int32 `json:"home"`
}

// SearchHit defines model for SearchHit.
type SearchHit struct {
	AwayCompetitorName *string `json:"away_competitor_name,omitempty"`
	HomeCompetitorName *string `json:"home_competitor_name,omitempty"`

	// Id A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	Id EntityID `json:"id"`

	// Kind Exactly `domain.EventKind`. `match` is a contest between two competitors;
	// `outright` is a competition resolved among many runners and has no home
	// or away side at all -- not empty ones.
	Kind EventKind `json:"kind"`

	// LeagueId A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	LeagueId       EntityID  `json:"league_id"`
	Name           string    `json:"name"`
	ScheduledStart time.Time `json:"scheduled_start"`

	// Status Exactly `domain.EventStatus`, same spelling, same members. The API does
	// not get to invent a status the domain cannot represent, and the domain
	// does not get to add one without changing this list.
	Status EventStatus `json:"status"`
}

// SearchPage defines model for SearchPage.
type SearchPage struct {
	Data []SearchHit `json:"data"`

	// Page Keyset pagination. There is no total count and there will not be one:
	// counting an unbounded, continuously-written set costs a full scan on
	// every page and the number is stale before it is serialised.
	Page PageInfo `json:"page"`
}

// Selection defines model for Selection.
type Selection struct {
	BestPrice *Price `json:"best_price,omitempty"`

	// Id A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	Id EntityID `json:"id"`

	// MarketId A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	MarketId EntityID `json:"market_id"`
	Name     string   `json:"name"`

	// Prices The current price at each quoting book, ordered by book slug. Empty
	// when no book has quoted this selection inside the freshness window --
	// which is a correct answer, not a missing one.
	Prices []Price `json:"prices"`

	// Role Exactly `domain.SelectionRole`.
	Role SelectionRole `json:"role"`
}

// SelectionQuote defines model for SelectionQuote.
type SelectionQuote struct {
	// DecimalOdds Decimal odds -- the total return per unit staked, stake included. THE
	// CANONICAL representation. American and fractional are derived and lossy.
	//
	//
	// Example: 1.909
	DecimalOdds        DecimalOdds `json:"decimal_odds"`
	Display            *string     `json:"display,omitempty"`
	ImpliedProbability Probability `json:"implied_probability"`
	IngestedAt         time.Time   `json:"ingested_at"`
	Line               *float64    `json:"line,omitempty"`
	ObservedAt         time.Time   `json:"observed_at"`

	// Role Exactly `domain.SelectionRole`.
	Role SelectionRole `json:"role"`

	// SelectionId A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	SelectionId EntityID `json:"selection_id"`
}

// SelectionRole Exactly `domain.SelectionRole`.
type SelectionRole string

// SessionResponse Returned by register, login and refresh. Both tokens are returned in the
// BODY and never set as cookies: this API is consumed cross-origin through
// a proxy by a Next.js client and by `pkg/client`, and a cookie-bearing
// API is a CSRF surface that would have to be defended with a second
// mechanism. The client stores the refresh token; the access token is held
// in memory.
//
// **The refresh token is shown exactly once.** It is not retrievable
// afterwards and it does not appear in any log, span or metric.
type SessionResponse struct {
	// AccessToken Short-lived JWT. Present it as `Authorization: Bearer <token>`.
	AccessToken string `json:"access_token"`

	// Account The whole profile. There is no name, address, date of birth, document,
	// country or jurisdiction field, because this is not a licensed sportsbook
	// and no such column exists (migration 00005). Their absence is deliberate.
	Account Account `json:"account"`

	// ExpiresIn Access-token lifetime in seconds. Minutes, not hours.
	ExpiresIn int32 `json:"expires_in"`

	// RefreshExpiresAt Absolute end of the login lineage, measured from when the family
	// STARTED, not from this token's issue. Without that, a session
	// rotated every ten minutes would live forever.
	RefreshExpiresAt time.Time `json:"refresh_expires_at"`

	// RefreshToken Opaque. Rotates on every use; reuse revokes the whole family.
	RefreshToken string                   `json:"refresh_token"`
	TokenType    SessionResponseTokenType `json:"token_type"`
}

// SessionResponseTokenType defines model for SessionResponse.TokenType.
type SessionResponseTokenType string

// SetLimitRequest Exactly one of `amount_minor` and `duration_seconds` must be present, and
// which one is determined by `kind`: `session` takes a duration, every
// other kind takes an amount. The database enforces the same biconditional,
// so an impossible combination is unstorable as well as rejected.
//
// Omitting both REMOVES the limit, which is a loosening and serves the
// cooling-off period like any other.
type SetLimitRequest struct {
	AmountMinor     *MoneyMinor `json:"amount_minor,omitempty"`
	DurationSeconds *int32      `json:"duration_seconds,omitempty"`

	// Kind Deliberately NOT `deposit`: this system has no deposits (CLAUDE.md
	// section 0). `grant`, `stake` and `loss` are spelled identically to the
	// ledger entry kinds they are evaluated against, so enforcing a limit is a
	// sum filtered by the same string with no translation table in between.
	Kind   LimitKind   `json:"kind"`
	Period LimitPeriod `json:"period"`
}

// Slug Example: nfl
type Slug = string

// Sport defines model for Sport.
type Sport struct {
	// Id A domain identifier. The charset is exactly `domain.validID`: `:` is
	// excluded because WebSocket channels are `event:{id}` and a colon inside
	// an identifier would make splitting a channel name ambiguous.
	//
	//
	// Example: evt_nfl_2026_w1_kc_buf
	Id   EntityID `json:"id"`
	Name string   `json:"name"`

	// Slug Example: nfl
	Slug Slug `json:"slug"`
}

// SportPage defines model for SportPage.
type SportPage struct {
	Data []Sport `json:"data"`

	// Page Keyset pagination. There is no total count and there will not be one:
	// counting an unbounded, continuously-written set costs a full scan on
	// every page and the number is stale before it is serialised.
	Page PageInfo `json:"page"`
}

// TOTPCodeRequest defines model for TOTPCodeRequest.
type TOTPCodeRequest struct {
	// Code Compared in constant time against the expected code for the current and adjacent time steps.
	Code string `json:"code"`
}

// TOTPEnrolment Returned ONCE. The shared secret is never retrievable afterwards and is
// stored only as AEAD ciphertext under a key held outside the database,
// with the user id as additional authenticated data so a row copied between
// users fails to decrypt rather than silently granting the attacker's
// device the victim's second factor.
type TOTPEnrolment struct {
	// ExpiresAt An unconfirmed enrolment expires. A stale one must not linger as a half-enrolled factor.
	ExpiresAt time.Time `json:"expires_at"`

	// ProvisioningUri `otpauth://totp/...`. CREDENTIAL MATERIAL -- it embeds the shared
	// secret. Never logged, never traced, never persisted by a client
	// beyond the moment the user scans it.
	ProvisioningUri string `json:"provisioning_uri"`
}

// BookFilter defines model for BookFilter.
type BookFilter = []Slug

// Cursor defines model for Cursor.
type Cursor = string

// EventID A domain identifier. The charset is exactly `domain.validID`: `:` is
// excluded because WebSocket channels are `event:{id}` and a colon inside
// an identifier would make splitting a channel name ambiguous.
//
// Example: evt_nfl_2026_w1_kc_buf
type EventID = EntityID

// LeagueSlug Example: nfl
type LeagueSlug = Slug

// MarketID A domain identifier. The charset is exactly `domain.validID`: `:` is
// excluded because WebSocket channels are `event:{id}` and a colon inside
// an identifier would make splitting a channel name ambiguous.
//
// Example: evt_nfl_2026_w1_kc_buf
type MarketID = EntityID

// OddsFormatQuery defines model for OddsFormatQuery.
type OddsFormatQuery = OddsFormat

// PageLimit defines model for PageLimit.
type PageLimit = int32

// SelectionID A domain identifier. The charset is exactly `domain.validID`: `:` is
// excluded because WebSocket channels are `event:{id}` and a colon inside
// an identifier would make splitting a channel name ambiguous.
//
// Example: evt_nfl_2026_w1_kc_buf
type SelectionID = EntityID

// SportSlug Example: nfl
type SportSlug = Slug

// StartingBefore defines model for StartingBefore.
type StartingBefore = time.Time

// BadRequest THE ONE ERROR SHAPE. Every non-2xx response in this API has exactly this
// body, so a client writes one decoder and one branch.
//
// `code` is a stable machine-readable token and is what a client should
// switch on; the HTTP status is a coarser classification of the same fact.
// `message` is a fixed human-readable string chosen from a closed set in
// Go. It is NEVER an error value, a driver message, a SQLSTATE, a stack
// frame, a file path or a query -- an error body is an untrusted output
// surface and everything a caller must not learn is logged under
// `request_id` and dropped here.
//
// `invalid_params` is present only for `400` and `422` and names the
// offending parameters so a form can highlight them. Its `reason` strings
// are equally fixed.
type BadRequest = Error

// InternalError THE ONE ERROR SHAPE. Every non-2xx response in this API has exactly this
// body, so a client writes one decoder and one branch.
//
// `code` is a stable machine-readable token and is what a client should
// switch on; the HTTP status is a coarser classification of the same fact.
// `message` is a fixed human-readable string chosen from a closed set in
// Go. It is NEVER an error value, a driver message, a SQLSTATE, a stack
// frame, a file path or a query -- an error body is an untrusted output
// surface and everything a caller must not learn is logged under
// `request_id` and dropped here.
//
// `invalid_params` is present only for `400` and `422` and names the
// offending parameters so a form can highlight them. Its `reason` strings
// are equally fixed.
type InternalError = Error

// NotFound THE ONE ERROR SHAPE. Every non-2xx response in this API has exactly this
// body, so a client writes one decoder and one branch.
//
// `code` is a stable machine-readable token and is what a client should
// switch on; the HTTP status is a coarser classification of the same fact.
// `message` is a fixed human-readable string chosen from a closed set in
// Go. It is NEVER an error value, a driver message, a SQLSTATE, a stack
// frame, a file path or a query -- an error body is an untrusted output
// surface and everything a caller must not learn is logged under
// `request_id` and dropped here.
//
// `invalid_params` is present only for `400` and `422` and names the
// offending parameters so a form can highlight them. Its `reason` strings
// are equally fixed.
type NotFound = Error

// TooManyRequests THE ONE ERROR SHAPE. Every non-2xx response in this API has exactly this
// body, so a client writes one decoder and one branch.
//
// `code` is a stable machine-readable token and is what a client should
// switch on; the HTTP status is a coarser classification of the same fact.
// `message` is a fixed human-readable string chosen from a closed set in
// Go. It is NEVER an error value, a driver message, a SQLSTATE, a stack
// frame, a file path or a query -- an error body is an untrusted output
// surface and everything a caller must not learn is logged under
// `request_id` and dropped here.
//
// `invalid_params` is present only for `400` and `422` and names the
// offending parameters so a form can highlight them. Its `reason` strings
// are equally fixed.
type TooManyRequests = Error

// Unauthorized THE ONE ERROR SHAPE. Every non-2xx response in this API has exactly this
// body, so a client writes one decoder and one branch.
//
// `code` is a stable machine-readable token and is what a client should
// switch on; the HTTP status is a coarser classification of the same fact.
// `message` is a fixed human-readable string chosen from a closed set in
// Go. It is NEVER an error value, a driver message, a SQLSTATE, a stack
// frame, a file path or a query -- an error body is an untrusted output
// surface and everything a caller must not learn is logged under
// `request_id` and dropped here.
//
// `invalid_params` is present only for `400` and `422` and names the
// offending parameters so a form can highlight them. Its `reason` strings
// are equally fixed.
type Unauthorized = Error

// UnprocessableEntity THE ONE ERROR SHAPE. Every non-2xx response in this API has exactly this
// body, so a client writes one decoder and one branch.
//
// `code` is a stable machine-readable token and is what a client should
// switch on; the HTTP status is a coarser classification of the same fact.
// `message` is a fixed human-readable string chosen from a closed set in
// Go. It is NEVER an error value, a driver message, a SQLSTATE, a stack
// frame, a file path or a query -- an error body is an untrusted output
// surface and everything a caller must not learn is logged under
// `request_id` and dropped here.
//
// `invalid_params` is present only for `400` and `422` and names the
// offending parameters so a form can highlight them. Its `reason` strings
// are equally fixed.
type UnprocessableEntity = Error

// GetBoardParams defines parameters for GetBoard.
type GetBoardParams struct {
	// StartingBefore Upper bound on `scheduled_start` (RFC 3339). Defaults to 24 hours from
	// the time the request is served, which is "today's board".
	StartingBefore *StartingBefore `form:"starting_before,omitempty" json:"starting_before,omitempty"`

	// Limit Maximum rows in this page.
	Limit *PageLimit `form:"limit,omitempty" json:"limit,omitempty"`

	// Cursor Opaque continuation token from a previous response's `page.next_cursor`.
	// Do not construct or parse one. A cursor is bound to the ordering and the
	// filters it was minted under and is rejected `400` if presented with
	// different ones.
	Cursor *Cursor `form:"cursor,omitempty" json:"cursor,omitempty"`

	// OddsFormat Adds a rendered `display` string to every price. `decimal_odds` is always
	// present regardless, because it is the canonical value and the other two
	// formats are lossy.
	OddsFormat *OddsFormatQuery `form:"odds_format,omitempty" json:"odds_format,omitempty"`

	// Book Restrict prices to these books, by slug. Repeatable. Omitted means every
	// book. An unknown slug is a `400`, not a silent empty result -- a typo
	// that quietly returns nothing is the worst possible outcome for a price
	// comparison.
	Book *BookFilter `form:"book,omitempty" json:"book,omitempty"`
}

// GetEventParams defines parameters for GetEvent.
type GetEventParams struct {
	// OddsFormat Adds a rendered `display` string to every price. `decimal_odds` is always
	// present regardless, because it is the canonical value and the other two
	// formats are lossy.
	OddsFormat *OddsFormatQuery `form:"odds_format,omitempty" json:"odds_format,omitempty"`

	// Book Restrict prices to these books, by slug. Repeatable. Omitted means every
	// book. An unknown slug is a `400`, not a silent empty result -- a typo
	// that quietly returns nothing is the worst possible outcome for a price
	// comparison.
	Book *BookFilter `form:"book,omitempty" json:"book,omitempty"`
}

// GetLeagueBoardParams defines parameters for GetLeagueBoard.
type GetLeagueBoardParams struct {
	// StartingBefore Upper bound on `scheduled_start` (RFC 3339). Defaults to 24 hours from
	// the time the request is served, which is "today's board".
	StartingBefore *StartingBefore `form:"starting_before,omitempty" json:"starting_before,omitempty"`

	// Limit Maximum rows in this page.
	Limit *PageLimit `form:"limit,omitempty" json:"limit,omitempty"`

	// Cursor Opaque continuation token from a previous response's `page.next_cursor`.
	// Do not construct or parse one. A cursor is bound to the ordering and the
	// filters it was minted under and is rejected `400` if presented with
	// different ones.
	Cursor *Cursor `form:"cursor,omitempty" json:"cursor,omitempty"`

	// OddsFormat Adds a rendered `display` string to every price. `decimal_odds` is always
	// present regardless, because it is the canonical value and the other two
	// formats are lossy.
	OddsFormat *OddsFormatQuery `form:"odds_format,omitempty" json:"odds_format,omitempty"`

	// Book Restrict prices to these books, by slug. Repeatable. Omitted means every
	// book. An unknown slug is a `400`, not a silent empty result -- a typo
	// that quietly returns nothing is the worst possible outcome for a price
	// comparison.
	Book *BookFilter `form:"book,omitempty" json:"book,omitempty"`
}

// CompareMarketPricesParams defines parameters for CompareMarketPrices.
type CompareMarketPricesParams struct {
	// OddsFormat Adds a rendered `display` string to every price. `decimal_odds` is always
	// present regardless, because it is the canonical value and the other two
	// formats are lossy.
	OddsFormat *OddsFormatQuery `form:"odds_format,omitempty" json:"odds_format,omitempty"`

	// Book Restrict prices to these books, by slug. Repeatable. Omitted means every
	// book. An unknown slug is a `400`, not a silent empty result -- a typo
	// that quietly returns nothing is the worst possible outcome for a price
	// comparison.
	Book *BookFilter `form:"book,omitempty" json:"book,omitempty"`
}

// SearchEventsParams defines parameters for SearchEvents.
type SearchEventsParams struct {
	// Q Competitor name prefix. Matched literally -- `%` and `_` are escaped
	// before the query, so a caller cannot turn a prefix search into a
	// leading-wildcard scan.
	Q string `form:"q" json:"q"`

	// Limit Maximum rows in this page.
	Limit *PageLimit `form:"limit,omitempty" json:"limit,omitempty"`

	// Cursor Opaque continuation token from a previous response's `page.next_cursor`.
	// Do not construct or parse one. A cursor is bound to the ordering and the
	// filters it was minted under and is rejected `400` if presented with
	// different ones.
	Cursor *Cursor `form:"cursor,omitempty" json:"cursor,omitempty"`
}

// GetSelectionHistoryParams defines parameters for GetSelectionHistory.
type GetSelectionHistoryParams struct {
	// Book The book whose quotes to return, by slug. Required: a chart mixing
	// two books' quotes on one series is not a line movement chart, it is
	// two of them overlaid, and the caller must say which it wants.
	Book Slug `form:"book" json:"book"`

	// From Inclusive lower bound on `observed_at` (RFC 3339).
	From time.Time `form:"from" json:"from"`

	// To Exclusive upper bound on `observed_at` (RFC 3339). Defaults to the
	// time the request is served. Half-open so adjacent windows tile
	// without double-counting the boundary quote.
	To *time.Time `form:"to,omitempty" json:"to,omitempty"`

	// Resolution Downsampling bucket width. `raw` returns stored quotes unaggregated.
	Resolution *HistoryResolution `form:"resolution,omitempty" json:"resolution,omitempty"`

	// MaxPoints Reject (422) rather than truncate if the window would exceed this many points.
	MaxPoints *int32 `form:"max_points,omitempty" json:"max_points,omitempty"`

	// OddsFormat Adds a rendered `display` string to every price. `decimal_odds` is always
	// present regardless, because it is the canonical value and the other two
	// formats are lossy.
	OddsFormat *OddsFormatQuery `form:"odds_format,omitempty" json:"odds_format,omitempty"`
}

// SetLimitJSONRequestBody defines body for SetLimit for application/json ContentType.
type SetLimitJSONRequestBody = SetLimitRequest

// RemoveTOTPJSONRequestBody defines body for RemoveTOTP for application/json ContentType.
type RemoveTOTPJSONRequestBody = TOTPCodeRequest

// ConfirmTOTPEnrolmentJSONRequestBody defines body for ConfirmTOTPEnrolment for application/json ContentType.
type ConfirmTOTPEnrolmentJSONRequestBody = TOTPCodeRequest

// LoginJSONRequestBody defines body for Login for application/json ContentType.
type LoginJSONRequestBody = LoginRequest

// LogoutJSONRequestBody defines body for Logout for application/json ContentType.
type LogoutJSONRequestBody = RefreshRequest

// RefreshSessionJSONRequestBody defines body for RefreshSession for application/json ContentType.
type RefreshSessionJSONRequestBody = RefreshRequest

// RegisterJSONRequestBody defines body for Register for application/json ContentType.
type RegisterJSONRequestBody = RegisterRequest
